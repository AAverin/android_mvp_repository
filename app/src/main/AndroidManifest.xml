<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="anton.averin.pro.tests.sampleapp">

    <application
        android:name="anton.averin.pro.tests.sampleapp.BaseContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="anton.averin.pro.tests.sampleapp.views.activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="anton.averin.pro.tests.sampleapp.views.activities.RecipesActivity" />

    </application>

</manifest>
